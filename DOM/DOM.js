

const rangoEtario= document.querySelector(".rangoEtario");

// document.write(rangoEtario) // nos muestra que efectivamente es una obejeto

rangoEtario.setAttribute("type", "text") //el primer parametro es lo que yo quiero modificarm y el segundo es el nuevo valor. En este caso modifico el type=range por un texto, ademas si yo le saco el atributo type tambien funciona ya que ademas los crea.